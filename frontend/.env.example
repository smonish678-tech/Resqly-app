# Frontend environment example — copy to .env.local for dev, or rely on .env.production for builds.

# Backend URL — for local dev with backend on same host, this can be /api proxy or absolute URL.
REACT_APP_BACKEND_URL=https://your-backend-url.example.com

# Optional flags
ENABLE_HEALTH_CHECK=false
GENERATE_SOURCEMAP=false
