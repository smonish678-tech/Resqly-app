# Generated by Capacitor — safe to commit android/ but ignore build outputs
android/.gradle/
android/build/
android/app/build/
android/.idea/
android/local.properties
android/captures/
android/app/release/
android/app/debug/
*.iml
.gradle/
build/

# Source assets are kept; generated mipmap PNGs in res/ are also kept since
# the generator deterministically reproduces them.

# Local signing keystore (NEVER commit real keystores)
*.jks
*.keystore
release-signing.jks
keystore.properties
