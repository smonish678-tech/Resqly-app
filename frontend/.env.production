# Production backend URL — baked into the APK at build time.
# This is the deployed Resqly backend (Emergent production deployment).
REACT_APP_BACKEND_URL=https://resqly-launch.emergent.host
ENABLE_HEALTH_CHECK=false
GENERATE_SOURCEMAP=false
